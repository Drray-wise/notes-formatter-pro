services:
  - type: web
    name: notes-formatter-mrwise
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cloud_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
